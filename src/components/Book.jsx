import React, { useState } from 'react';

function Book({ book }) {
    const [showMore, setShowMore] = useState(false);

    return (
        <div>
            
        </div>
    )
}

export default Book;